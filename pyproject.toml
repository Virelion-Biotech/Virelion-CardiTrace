[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "virelion-cardi-trace"
version = "0.2.0"
description = "High-assurance provenance, lineage, reproducibility, audit, federation, and traceability for Virelion cardiac AI workflows."
readme = "README.md"
requires-python = ">=3.10"
authors = [{name = "Virelion Biotech"}]
license = {file = "LICENSE"}
keywords = ["provenance", "lineage", "reproducibility", "audit", "federation", "telemetry", "cardiac", "biomedical-ai"]

[project.optional-dependencies]
test = ["pytest>=8"]

[project.scripts]
carditrace = "cardi_trace.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
